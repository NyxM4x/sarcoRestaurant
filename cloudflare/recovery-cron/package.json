{
  "name": "sarco-recovery-cron",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Cron único de Sarco: cada minuto despierta en paralelo los cuatro endpoints internos de recuperación de Vercel.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5",
    "typescript": "^5",
    "vitest": "^4.1.10",
    "wrangler": "^4"
  }
}
