{
  "name": "sarco-notification-recovery-cron",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Cron EXCLUSIVO de Sarco que despierta el worker interno de recuperación de notificaciones (POST {} al endpoint de Vercel de Sarco). No es compartido: cada despliegue tiene el suyo, con nombre propio, para no colisionar en la misma cuenta de Cloudflare.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5",
    "typescript": "^5",
    "vitest": "^4.1.10",
    "wrangler": "^4"
  }
}
