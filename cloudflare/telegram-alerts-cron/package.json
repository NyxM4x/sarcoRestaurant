{
  "name": "sarco-telegram-alerts-cron",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Cron dedicado que despierta el outbox de alertas de Telegram (POST {} al endpoint interno de Vercel). Despliegue independiente de los otros dos crons.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5",
    "typescript": "^5",
    "vitest": "^4.1.10",
    "wrangler": "^4"
  }
}
